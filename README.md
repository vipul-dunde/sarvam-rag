### Warning: Intentinally pushing `.env` file to the repository for the sake of simplicity and to run locally.

### use your ```OPENAI_API_KEY``` in the `.env` file to run with OpenAI flow locally.

## Getting Started

First, run the development server:

```bash
npm install
npm run build
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.